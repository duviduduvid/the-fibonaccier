import { isBigIntSupported, getNthFibonacci } from './fibonacci-service';
import { getCurrentIndex, saveCurrentIndex } from './storage-service';

export {
    isBigIntSupported,
    getNthFibonacci,
    getCurrentIndex,
    saveCurrentIndex,
};
